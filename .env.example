# MySQL settings (used by docker-compose and the Go app)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=admin_sql
DB_PASSWORD=admin_sql
DB_NAME=goland_demo

# Optional: override the MySQL root password when using docker-compose
DB_ROOT_PASSWORD=root_pass
